apiVersion: v1
kind: Service
metadata:
  name: krsxt
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "krsxt"
    port: 8080
  selector:
    app: krsxt
