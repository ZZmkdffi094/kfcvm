apiVersion: v1
kind: Service
metadata:
  name: wedxk
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "wedxk"
    port: 8080
  selector:
    app: wedxk
